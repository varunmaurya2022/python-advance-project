#index():-This method returns position number of first occurrenece of given element in the array.
#If it doesn't found the element, shows valueError.
#Syntax:-
from array import*
stu=('i',[101,102,103,104,105])
print(stu)
print(stu.index(101))